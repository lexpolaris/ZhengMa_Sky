# 郑码天空（ZhengMa Sky）

一个跨平台的郑码输入法训练软件，基于对原 Windows 版《郑码天空》的逆向分析重新实现。

---

## 项目简介

《郑码天空》是一款帮助用户学习和练习**郑码输入法**的辅助软件。原版由王安惠于 2006 年使用 Borland C++ Builder 开发，仅支持 Windows。

本项目通过逆向分析原程序的**数据格式、训练算法、单元体系**，使用 **Qt 6 + C++17** 重新实现，支持跨平台。

**核心特点**：
- 完整的郑码学习课程体系（44 个单元）
- 基于**末位训练**算法的智能题目调度
- 使用原版数据（逆向解析 `user0.dat` 和 `zmmb.en` 取得）
- 跨平台原生界面

---

## 功能

### 训练模式

- **单元训练**：按单元逐题练习，每轮只练最不熟悉的 N 个题目
- **单元测试**：全屏计时，记录成绩
- **游戏闯关**（部分实现）：难度递进

### 末位训练算法

原版的核心设计。每个题目有**速度值**（反应时间，ms），每轮按速度排序，只训练最慢的 N 个：

```
1. 按速度从慢到快排序
2. 对前 N 个做 Fisher-Yates 洗牌
3. 依次出题
4. 记录反应时间，更新速度表
5. 一轮结束后重新排序，开始下一轮
```

错误题目被标记为 `50002`（大于任何正常值），下轮必被选中。

### 单元体系

按学习阶段分为 5 大类，共 44 个单元：

| 分类 | 单元数 | 说明 |
|------|--------|------|
| 字根单元 | 12 | 笔画、主根、副根、全部字根 |
| 简码单元 | 11 | 一级/二级/三级简码、组合 |
| 词汇单元 | 4 | 二字词、三字词、四字词、多字词 |
| 常用单元 | 6 | 常用字/词、冠军字、核心字词 |
| 提速单元 | 8 | 按基根数分类的字词 |

### 测试记录

- 保存每个单元最近 10 次测试记录
- 绘制速度 / 成绩曲线（Qt Charts）
- 可复制历史记录到剪贴板

### 参数设置

- **显示方式**：大写 / 小写
- **快捷键**：自定义显示/隐藏窗口的热键
- **末位训练数量**：每轮训练题数
- **末位训练次数**：自动 / 手动
- **测试题目数量**：自动 / 手动

---

## 数据格式

程序使用两份**数据文件**（UTF-8 编码），与原版兼容。

### `data/user0.xml` — 用户数据

包含所有单元、题目、用户配置。

```xml
<?xml version='1.0' encoding='utf-8'?>
<ZmSoft StatusName='Train' Score='0' Date='2006-07-30' 
         DisplayType='0' TailTrainMax='3' HotKey='121' 
         ShowSimilarRoot='0' WindowsLayout='0' 
         AutoTailTrainCount='0' ShowHitSpeed='1'>
  <Train LibName='笔画' Grade='1' .../>
  <Test  LibName='笔画' .../>
  <Game  LibName='笔画' .../>
  <Libs>
    <Lib LibName='笔画' LibNo='0' Count='24' ...>
      <b c='一' m='A' a='横区：A-H'/>
      <b c='丨' m='I' a='竖区：I-L'/>
      ...
    </Lib>
    ...
  </Libs>
</ZmSoft>
```

**题目属性**：

| 属性 | 含义 |
|------|------|
| `c` | 字 / 词 / 字根 |
| `m` | 郑码编码（可能为空，需查码表） |
| `s` | 拆分说明 |
| `a` | 联想记忆提示 |

### `data/zmmb.txt` — 郑码码表

`字/词` + Tab + `编码`，多个编码用空格分隔。

```
〇	jaa
一	a
一一	avav
一下	aa avai
一丘之貉	apwp
```

### `data/zmzg.ttf` — 字根字体

原版自带的自定义字体，用于显示标准字体无法渲染的郑码字根。

### `data/help.txt` — 帮助文档

XML 格式，原版帮助。

### `data/test_history.xml` — 测试记录

程序运行时生成，保存测试历史。

---

## 编译

### 依赖

- **Qt 6.5+**（Widgets、Xml、Charts）
- **CMake 3.16+**
- **C++17 编译器**（GCC 9+ / Clang 10+ / MSVC 2019+）

---

## 核心算法

### 判定

```
正确答案 = " " + 编码 + " "
用户输入 = " " + 输入 + " "
判定：正确答案.contains(用户输入)
```

多个编码任意匹配一个即可。

### 速度计算

```
速度（字/分钟）= 60000 × 字数 / 总时间(ms)
等级 = 速度 / 10
```

### 末位训练题数

**训练模式**（`TailTrainItemsCount`）：

```
if 单元编号 < 200:
    if 题数 < 15:    N = 题数
    elif 题数 < 100: N = 题数 / 2
    elif 题数 < 200: N = 题数 / 3
    elif 题数 < 300: N = 题数 / 3
    elif 题数 < 500: N = 题数 / 4
    else:            N = 200
else:                N = 50
```

**测试模式**（`TailTestItemsCount`）：

```
if 题数 < 50:    N = 2 × 题数
elif 题数 < 400: N = 题数
else:            N = 300
```

**用户可在参数设置里覆盖测试题数。**

---

## 快捷键

| 快捷键 | 功能 |
|--------|------|
| `Enter` | 提交答案 / 进入测试 |
| `Esc` | 进入训练模式 |
| `F1` | 显示测试记录 |
| `F2` | 显示测试曲线 |
| `F5` | 切换窗口布局 |
| `F10` | 显示/隐藏窗口（可自定义） |

---

## 与原文的差异

| 项目 | 原版 | 本项目 |
|------|------|--------|
| 平台 | Windows only | QT跨平台 |
| 框架 | Borland VCL | Qt 6 Widgets |
| 数据 | 加密（LCG + XOR） | 明文 UTF-8 |
| 图表 | TeeChart | Qt Charts |
| 打包 | 单文件 EXE | CMake 多平台 |
| 启动画面 | 有 | 无 |
| 帮助系统 | RichEdit | QTextEdit |
| 字体渲染 | GDI | Qt 原生 |

---

## 已知限制

- **热键注册**：仅在 Windows 上全局生效，Linux/macOS 需额外配置
- **帮助系统**：仅加载 `help.txt`，未接入单元帮助（`unithelp.txt`）
- **多用户支持**：原版支持 `user0/user1/...`，本项目暂未实现
- **数据持久化**：训练进度未写回 `user0.xml`，仅保存测试记录

---

## 致谢

- **原作者**：上水，2006
- **郑码发明人**：郑易里教授、郑珑高级工程师
- **郑码资源**：中易公司、郑码爱好者家园论坛

---

## 许可

本项目仅用于**学习和研究**，不得用于商业目的。

原版《郑码天空》的版权归原作者所有。郑码输入法的相关权利归郑码著作权人所有。

---

**祝你在郑码的天空中自由飞翔。**